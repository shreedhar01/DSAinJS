// Understanding Truthy and Falsy in JavaScript:
// Truthy values: Non-zero numbers (including negative numbers), non-empty strings, objects, arrays, true.

// Falsy values: false, 0, "" (empty string), null, undefined, NaN.


// function check(a){
//     return (a || 0);
// }

// console.log(check(null));

// const count = new Array(26).fill(0);
// let str = "hello"
// console.log(str.charCodeAt(3))

let help = [""]
console.log((help.join(":;")).length)
console.log(help.length)